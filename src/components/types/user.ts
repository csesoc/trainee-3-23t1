export type CleanUserType = {
  name: string;
  handleStr: string;
  email: string;
  id: string;
  image: string;
};

export type MinimalCleanUserType = {
  handle: string;
  id: string;
  image: string;
};
